'use client'

function CustomButton() {
  return (
    <button
      disabled={false}
      type={'button'}
      className={`custom-btn`}
      onClick={()=>{}}
      >
      <span className={`fex-1`}></span>
    </button>
  )
}

export default CustomButton